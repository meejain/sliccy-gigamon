export default async function decorate(block) {
  // The announcement bar is a simple sticky CTA bar
  // Content is a single link styled as a button by EDS
  // No additional DOM restructuring needed
}
