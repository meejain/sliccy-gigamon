export default async function decorate(block) {
  // The footer-links block has 3 rows:
  // Row 1: 4 column link groups
  // Row 2: social links + legal links
  // Row 3: copyright
  // No special restructuring needed — CSS handles the layout.
}
