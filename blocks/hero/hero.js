export default async function decorate(block) {
  // The block has one row with two cells:
  // Cell 1: background image (picture)
  // Cell 2: text content (h1, p, CTA link)
  // No DOM restructuring needed — CSS handles the layout
  // with absolute positioning for the image and relative for text.
}
